services:
  mongodb:
    image: mongo:7
    container_name: linkedin-mongodb
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: linkedin_jobs
    volumes:
      - mongodb_data:/data/db

volumes:
  mongodb_data: